export let fullScreen: boolean = (window.innerWidth > 768) ? true : false;
export let info: { userId: string, roomId: string } = { userId: 'sdf', roomId: '-1' }
// export let userId: number = 0
// export let roomId: number = 0
